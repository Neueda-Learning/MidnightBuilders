package com.example.demo.service;

/**
 * Deprecated legacy placeholder kept only to avoid breaking historical references.
 *
 * <p>当前生效的业务实现是 {@link PaymentService}。该类不参与 Spring Bean 注册，
 * 仅作为兼容占位文件保留，避免旧文档或旧分支引用路径时报错。</p>
 */
public final class PaymentServiceImpl {

    private PaymentServiceImpl() {
        // 工具类占位：禁止实例化。
    }
}
